import { Inngest } from "inngest";

// Create a client to send and receive events
export const inngest = new Inngest({ id: "quickcart-next" });

// Supabase and Clerk logic removed as per user request.
// If you need to sync data with Shopify or another provider, implement new functions here.
